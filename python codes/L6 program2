#WAF to print the length of a list. ( list is the parameter)
cities=["mumbai","delhi","gurgaon","noida"]
heroes=["superman","batman","thor","hawk","hulk","captain america"]

def print_len(list):
    print(len(list))

print_len(cities)
print_len(heroes)

#WAF to print the elements of a list in a single line. ( list is the parameter)
heroes=["superman","batman","thor","hawk","hulk","captain america"]

def print_list(list):
    for item in list:
        print(item, end=" ")        # end=" " === print all items on single line but with space 

print_list(heroes)
print()

#WAF to find the factorial of n. (n is the parameter)
     # Method-1 without function:

n=5                                       #5! = 1*2*3*4*5==120
fact=1
for i in range(1,n+1):
    fact*=i
print(fact)

     # Method-2 with fuction :
def cal_fact(n):
    fact=1
    for i in range(1,n+1):
        fact*=i
    print(fact)

cal_fact(5)

#WAF to convert USD to INR.
def converter(usd_val):
    inr_val=usd_val*83
    print(usd_val, "USD =",inr_val, "INR ")

converter(100)